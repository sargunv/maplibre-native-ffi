# Pinned commit of MapLibre Native to fetch when no local checkout is provided.
# The CI cache key for the fetched source tree is derived from this file, so
# bumping this SHA here is what invalidates the cache.
set(MLN_GIT_TAG "647636bf6115e058991dd644638ba6748c9cf4c6")
