[env]
PKG_CONFIG_PATH = "{{config_root}}/../../build/pkgconfig"

[tasks.ci]
run = ["mise run :build"]

[tasks.build]
depends = ["//:ensure-native-library"]
run = "swift build"

[tasks.run]
depends = ["build"]
run = "swift run"
